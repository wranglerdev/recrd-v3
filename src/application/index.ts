// Application layer — use cases orchestrating the domain (platform-agnostic).
// Depends on `domain`; never imports Electron/Node or outer layers.
// Populated by the use-case epics; intentionally empty for now.
export {};
