// Renderer layer — React + TypeScript UI. Communicates with the main process
// exclusively through the typed preload bridge. The React entry point and root
// component are added by the UI epics; intentionally empty for now.
export {};
